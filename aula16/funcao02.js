function soma (n1=0, n2=0){//Os paramentros estão recebendo o valor 0, pois caso não seja passado valores, esses não ficarão unde.
    return n1 + n2
}

console.log(soma(10,56)) // caso seja removido algum valor, os paramentros estão definido com o valor 0, então a soma irá prosseguir sem erro, ou seja, não ficará undefined...